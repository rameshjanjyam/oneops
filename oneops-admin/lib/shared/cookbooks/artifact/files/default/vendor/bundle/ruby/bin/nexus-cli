#!/usr/bin/env ruby
#
# This file was generated by RubyGems.
#
# The application 'nexus_cli' is installed as part of a gem, and
# this file is here to facilitate running it.
#

require 'rubygems'

version = ">= 0"

if ARGV.first
  str = ARGV.first
  str = str.dup.force_encoding("BINARY") if str.respond_to? :force_encoding
  if str =~ /\A_(.*)_\z/
    version = $1
    ARGV.shift
  end
end

gem 'nexus_cli', version
load Gem.bin_path('nexus_cli', 'nexus-cli', version)
